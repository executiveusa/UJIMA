[build]
  command = "npm run build --workspace @asc3nd/site"
  publish = "apps/site/.next"

[build.environment]
  NODE_VERSION = "24"
  NEXT_PUBLIC_UJIMA_PRODUCT = "ujima"
  NEXT_PUBLIC_UJIMA_DEFAULT_TENANT = "public"

[dev]
  command = "npm run dev:web"
  targetPort = 3000
  port = 8888
