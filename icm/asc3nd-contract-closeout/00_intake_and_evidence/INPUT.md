# Stage 00 — Inputs

Permitted sources:

1. Exact $2,450 contract copy supplied by the user
2. Interactive workbook repository and database evidence
3. Session recordings, notes, calendar entries, and transcripts
4. Raw interview footage and original audio
5. Existing approved ASC3ND copy and brand assets
6. Current production and preview URLs
7. Existing social strategy, calendars, captions, bios, shot lists, and exports
8. Git history, PRs, CI, and deployment evidence

Do not use:

- model memory as proof;
- inferred session completion;
- design prompts as client approval;
- generated summaries where exact contract wording is required;
- the currently connected `botanic-creations` Supabase project as ASC3ND data.

Missing-input protocol:

- Continue all independent work.
- Add the missing item to `STATUS.json.blockers`.
- Ask the user only when the missing item blocks the active stage.
- Ask one consolidated question, not repeated questions across agents.